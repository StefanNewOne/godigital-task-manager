pnpm exec lint-staged

# Secret scanning (optional — runs only if gitleaks is installed).
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact --no-banner
else
  echo "gitleaks not installed — skipping secret scan (install it before pushing secrets-sensitive work)"
fi
